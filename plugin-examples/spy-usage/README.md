# spy-usage

Claude and Cursor plan usage in the Paseo sidebar.

```bash
cd plugin-examples/spy-usage
npm install
npm run typecheck
paseo plugin install /absolute/path/to/plugin-examples/spy-usage
```
